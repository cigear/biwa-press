/*
import type { Locale } from '$lib/config/locales';
import type { Group } from '$lib/core/entities/sidebar';
import { DocRepository } from '$lib/infrastructure/storage/doc-repository';
*/

/**
 * 这是一个 Use Case：它定义了获取侧边栏的业务流程。
 * 它不关心 Markdown 是怎么解析的，只关心结构。
 */
/*
export async function getSidebarUseCase(locale: Locale): Promise<Group[]> {
  // 1. 调用 Repository 获取原始数据
  // 2. 执行排序、分类、标题提取逻辑
  // 3. 返回业务所需的结构
  // (逻辑从 docs.server.ts 的 scanDocs 迁移至此)
  return []; 
}
*/
