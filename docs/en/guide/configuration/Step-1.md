---
title: "Step 1"
description: "Configuration : Step 1"
order: 1
---

```mermaid
graph TD;
    A[开始] --> B{是否成功?};
    B -- 是 --> C[结果 A];
    B -- 否 --> D[结果 B];
```


```mermaid
graph TD;
    A[开始] --> B{是否成功?};
    B -- 是 --> C[结果 A];
    B -- 否 --> D[结果 B];
```


