---
title: はじめる
description: 最初の Biwa Press ドキュメントサイトを作成して実行します。
---

# はじめる

依存関係をインストールします。

```bash
npm install
```

開発サーバーを起動します。

```bash
npm run dev
```

静的サイトをビルドします。

```bash
npm run build
```

静的ファイルは `build/` に出力されます。
